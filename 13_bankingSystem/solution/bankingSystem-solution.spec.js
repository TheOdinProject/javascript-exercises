/// This is an exercise for the user practicing to create their own test cases

const bankingSystem = require('./bankingSystem-solution');

describe('bankingSystem', () => {
  test('First test description', () => {
    // Replace this comment with any other necessary code, and update the expect line as necessary

    expect(bankingSystem()).toBe('');
  });

  test('Second test description', () => {
    // Replace this comment with any other necessary code, and update the expect line as necessary

    expect(bankingSystem()).toBe('');
  });
});
